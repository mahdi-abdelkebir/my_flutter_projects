class Article {
  var code;
  var libelle;
  var quantite;
  var category;
  var etat;
  var typeVente;

  Article(this.code, this.libelle, this.quantite, this.category, this.etat,
      this.typeVente);
}
